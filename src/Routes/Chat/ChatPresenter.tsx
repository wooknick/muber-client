import React from "react";
import styled from "styled-components";
import Header from "../../Components/Header";

const Container = styled.div``;

const ChatPresenter: React.FunctionComponent = () => (
  <Container>
    <Header title={"Chat"} />
  </Container>
);

export default ChatPresenter;
